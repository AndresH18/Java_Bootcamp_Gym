spring:
  application:
    name: reporting-service

#  config:
#    import: "configserver:http://localhost:8888"
  #  cloud:
  #    config:
  #      username: <username>
  #      password: <password>
server:
  port: 8082

logging:
  level:
    root: info

